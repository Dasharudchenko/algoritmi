a = ['q', 'w', 'e', 'r', 't', 'y', 'u', 'i', 'o', 'p']
def linear_search(lst, x):
	lst.append(x)
	i = 0
	while lst[i] != x:
		print(i)
		i += 1
	return i if i < len(lst) - 1 else None

print('='*10, linear_search(a, 'i'), sep='\n')
